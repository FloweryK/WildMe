import { createTheme } from "@mui/material";

export const defaultTheme = createTheme({
  palette: {
    primary: {
      main: "rgba(71, 89, 236, 0.9)",
      // main: "#FFA53B",
    },
    secondary: {
      main: "rgba(242, 242, 242, 0.6)",
    },
  },
});
