import { createTheme } from "@mui/material";

export const defaultTheme = createTheme({
  palette: {
    primary: {
      main: "#596aee",
      // main: "#FFA53B",
    },
    secondary: {
      main: "rgba(242, 242, 242, 0.7)",
    },
  },
});
